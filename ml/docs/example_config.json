{
  "comment": "Example configuration file for PD Signal Classifier. Note: data_dir will use environment variable ML_DATA_DIR if set, otherwise fallback to default path.",
  "img_width": 100,
  "output_root": "outputs",
  
  "learning_rate": 0.001,
  "batch_size": 16,
  "epochs": 50,
  "k_folds": 5,
  
  "conv_filters": [16, 32, 64],
  "dense_units": [128, 64],
  "conv_dropout": 0.2,
  "dense_dropout": [0.3, 0.2],
  "l2_regularization": 0.001,
  "use_batch_norm": true,
  
  "early_stopping_patience": 10,
  "lr_reduction_patience": 5,
  "lr_reduction_factor": 0.5,
  "use_class_weights": true
}